# Shared-world ruling (read on resume; pause still holds)
Operator ruled PRODUCT: real ViaggiaTreno data. Your threat model gains a
real asset: authenticity of samples relative to a real upstream. Anchor
options now include origin-side signing at the proxy (if the proxy infra is
approved) vs client spot-checks against own budgeted fetches. A real-data
epic will precede implementation. Pause still holds.
